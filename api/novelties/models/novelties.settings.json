{
  "kind": "collectionType",
  "collectionName": "novelties",
  "info": {
    "name": "novelties",
    "description": ""
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "attributes": {
    "name": {
      "type": "string"
    },
    "url": {
      "type": "string"
    },
    "status": {
      "type": "enumeration",
      "enum": [
        "want",
        "purchased",
        "shipped",
        "delivered"
      ]
    }
  }
}
